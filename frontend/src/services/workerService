import api from "../api/axios";

export const getWorkers = async () => {
  const res = await api.get("/api/worker");
  return res.data;
};

export const createWorker = async (payload) => {
  return api.post("/api/worker/add", payload);
};
